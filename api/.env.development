# Elixir API Development Environment
MIX_ENV=dev

# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_DB=api_dev

# Phoenix
PHX_HOST=localhost
PORT=4000
SECRET_KEY_BASE=dev_secret_key_base_that_is_at_least_64_characters_long_enough_for_dev
GUARDIAN_SECRET_KEY=dev_guardian_secret_key_for_jwt_authentication

# Optional
POOL_SIZE=10

# Auth gRPC Service
AUTH_GRPC_HOST=auth
AUTH_GRPC_PORT=50051
